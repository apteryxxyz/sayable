{
  "name": "@sayable/swc-plugin",
  "version": "0.0.0",
  "main": "./dist/sayable.wasm",
  "scripts": {
    "check": "cargo check",
    "lint": "cargo clippy",
    "format": "cargo fmt",
    "build": "cargo build --release --target wasm32-wasip1 && shx mkdir -p dist && shx cp target/wasm32-wasip1/release/sayable.wasm dist/",
    "prepack": "pnpm build"
  }
}
