{
  "name": "@sayable/format-po",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "tsdown",
    "prepack": "pnpm build"
  },
  "exports": {
    ".": {
      "types": "./dist/formatter.d.mts",
      "default": "./dist/formatter.mjs"
    }
  },
  "files": ["dist", "!dist/**/*.map"],
  "dependencies": {
    "date-fns": "^4.1.0",
    "pofile": "^1.1.4"
  },
  "peerDependencies": {
    "@sayable/config": "*"
  },
  "devDependencies": {
    "@sayable/config": "workspace:^"
  }
}
