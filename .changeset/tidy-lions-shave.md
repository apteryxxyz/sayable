---
'saykit': minor
---

Take a record of messages keyed by locale in `createCatalogue`, dropping `locales` and `defaultLocale`: the keys are the locales and the first is the fallback `match` resolves to
