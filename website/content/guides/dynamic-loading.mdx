---
title: Dynamic loading
description: Load locale catalogues lazily to keep bundles small
---

SayKit gives you two ways to wire messages into a catalogue:

- **Inline**, pass `messages` directly. Every locale is bundled, ready to use.
- **Loader**, pass a `loader` function. Locales load on demand.

Most production apps land somewhere in between, preload one locale, lazy-load the rest. This guide covers the patterns.

## The trade-off

import { Package, Zap } from 'lucide-react';

<Cards>
  <Card icon={<Package />} title="Inline">

**Pros:** zero loading state, always-available, simpler runtime.<br />
**Cons:** every locale ships in the bundle (or initial chunk).<br />
**Best for:** small numbers of locales, server-rendered apps, CLIs.

  </Card>

  <Card icon={<Zap />} title="Loader">

**Pros:** only the active locale lives in memory, smaller initial load.<br />
**Cons:** async transitions on locale switch, requires bundler code-splitting.<br />
**Best for:** many locales, large catalogues, browser-only apps.

  </Card>
</Cards>

## Inline pattern

The simplest setup:

```ts title="src/i18n.ts"
import { createCatalogue } from 'saykit';
import en from './locales/en.po';
import fr from './locales/fr.po';

const catalogue = createCatalogue({
  locales: ['en', 'fr'],
  messages: { en, fr },
});

export default catalogue;
```

Both catalogues are part of the bundle. `catalogue.locale('fr')` is synchronous, no loading state ever.

## Loader pattern

```ts title="src/i18n.ts"
import { createCatalogue } from 'saykit';

const catalogue = createCatalogue({
  locales: ['en', 'fr', 'ja', 'de'],
  loader: async (locale) => {
    const mod = await import(`./locales/${locale}.po`);
    return mod.default;
  },
});

export default catalogue;
```

Bundlers turn the dynamic `import()` into a separate chunk per locale. The catalogue for the active locale loads on first use:

```ts
await catalogue.load('fr');
const say = catalogue.locale('fr');
```

The shape of the dynamic import (`./locales/${locale}.po`) matters, most bundlers (Vite, Webpack, Rolldown, esbuild) can statically analyse this pattern and produce one chunk per literal locale.

<Callout type="warn">
  `babel-plugin-saykit` currently rewrites **static** `import en from './locales/en.po'` only. For
  dynamic imports, the bundler does the work, the formatter still parses the file, but through the
  bundler's loader pipeline rather than SayKit's. This works fine for `unplugin-saykit` (Vite,
  Rolldown, Rollup, Webpack, &hellip;).
</Callout>

## Hybrid pattern

Preload the source locale, lazy-load everything else:

```ts title="src/i18n.ts"
import { createCatalogue } from 'saykit';
import en from './locales/en.po';

const catalogue = createCatalogue({
  locales: ['en', 'fr', 'ja', 'de'],
  messages: { en },
  loader: async (locale) => {
    const mod = await import(`./locales/${locale}.po`);
    return mod.default;
  },
});

export default catalogue;
```

Initial render is synchronous, the source locale is always there. Switching locales triggers a single network fetch (or chunk load), and you control when:

```ts
async function switchTo(locale: string) {
  await catalogue.load(locale);
  return catalogue.locale(locale);
}
```

## Preloading on hover

In a locale switcher, prefetch the catalogue on hover so the actual switch is instant:

```tsx
function LocaleOption({ locale }: { locale: string }) {
  return (
    <a href={`/${locale}`} onMouseEnter={() => catalogue.load(locale)}>
      {locale}
    </a>
  );
}
```

Calling `load()` on an already-loaded locale is a no-op, so this is safe to call repeatedly.

## Server-side

On the server, you usually load only the locale this request needs:

```ts
import { createCatalogue } from 'saykit';

const catalogue = createCatalogue({
  locales: ['en', 'fr', 'ja', 'de'],
  loader: (l) => import(`./locales/${l}.po`).then((m) => m.default),
});

export async function getSayFor(locale: string) {
  const matched = catalogue.match(locale);
  await catalogue.load(matched);
  return catalogue.locale(matched);
}
```

There is nothing to isolate. The catalogue has no current locale to leak between requests, and the view it hands back cannot be changed, so it is safe to pass anywhere.

## Sync loaders

Loaders don't have to return a promise. If you have everything available synchronously, say you build catalogues at request start, or you're in a CLI, return the object directly:

```ts
const catalogue = createCatalogue({
  locales: ['en', 'fr'],
  loader: (locale) => buildCatalogueFor(locale), // sync
});

catalogue.load('fr'); // returns undefined, not a promise
```

When all loaders are synchronous, `load()` returns synchronously too.

## Next

- [Runtime](/core-concepts/runtime), the full catalogue and view API
- [Locale detection](/guides/locale-detection), deciding what to load
